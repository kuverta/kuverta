/target
**/*.rs.bk
.env
.env.local
/data
/docker/dovecot/mail
*.db
*.db-shm
*.db-wal
.DS_Store
/.devdata

# The shared triage surface, copied in from the fuckbird repository by
# `make triage-ui`. Generated, so it is not source here — keeping a second
# copy of those files in this history would rot.
/apps/desktop/ui/fuckbird
